PORT=3001
NODE_ENV=development

# PostgreSQL connection string
DATABASE_URL=postgres://postgres:postgres@localhost:5432/innoinvest

# Sync model schema to the database on startup (code-first). Use only in development.
DB_SYNC=true
